<ui:ContentDialog x:Class="SistemaGVP.WPF.Views.Dialogs.CustomerSelectionDialog"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                  xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                  Title="Seleccionar Cliente"
                  PrimaryButtonText="Seleccionar"
                  CloseButtonText="Cancelar"
                  DialogMaxWidth="600"
                  DialogMaxHeight="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox Grid.Column="0"
                         PlaceholderText="Buscar cliente por nombre, RUC o teléfono..."
                         Text="{Binding CustomerSearchTerm, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,8,0" />
            <ui:Button Grid.Column="1"
                       Content="Buscar"
                       Appearance="Primary"
                       Width="100"
                       Command="{Binding SearchCustomersCommand}" />
        </Grid>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding CustomerSearchResults}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  AlternatingRowBackground="{DynamicResource DataGridRowAltBrush}"
                  AlternationCount="2"
                  SelectionMode="Single"
                  Style="{StaticResource DataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="RUC" Binding="{Binding TaxId}" Width="120" />
                <DataGridTextColumn Header="Teléfono" Binding="{Binding Phone}" Width="120" />
            </DataGrid.Columns>
            <DataGrid.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick"
                              Command="{Binding SelectCustomerCommand}"
                              CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
            </DataGrid.InputBindings>
        </DataGrid>
    </Grid>
</ui:ContentDialog>